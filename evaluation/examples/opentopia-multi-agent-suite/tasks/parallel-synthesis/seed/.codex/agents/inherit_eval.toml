name = "inherit_eval"
description = "Evaluation worker that inherits the parent thread provider and model."
developer_instructions = "Complete the delegated read-only task, use only the evidence named by the parent, and return a concise evidence-backed result. Do not create additional agents."
sandbox_mode = "read-only"
