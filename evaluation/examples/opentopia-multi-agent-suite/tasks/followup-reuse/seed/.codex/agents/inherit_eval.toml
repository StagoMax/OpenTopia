name = "inherit_eval"
description = "Evaluation worker that inherits the parent thread provider and model."
developer_instructions = "Complete the delegated read-only task and preserve your canonical identity when a follow-up restarts you. Do not create additional agents."
sandbox_mode = "read-only"
