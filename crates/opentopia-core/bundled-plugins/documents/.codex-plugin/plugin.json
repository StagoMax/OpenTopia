{
  "name": "documents",
  "version": "1.3.0",
  "description": "Inspect, extract, and validate DOCX files through OpenTopia's native document tool.",
  "author": {
    "name": "OpenTopia Contributors"
  },
  "interface": {
    "displayName": "Documents",
    "shortDescription": "Read DOCX files safely for the model.",
    "longDescription": "Give the model bounded DOCX inspection, extraction, and validation without rewriting the source file. Desktop file preview is an independent application capability.",
    "developerName": "OpenTopia",
    "category": "Productivity",
    "defaultPrompt": "Use the Document tool when the task requires deterministic DOCX inspection, extraction, or validation.",
    "capabilities": [
      "DOCX package inspection",
      "Bounded text extraction",
      "Preservation risk validation"
    ]
  },
  "opentopia": {
    "apiVersion": "1",
    "requires": {
      "hostCapabilities": [
        "workspace.files.v1",
        "nativeTool.document.v1"
      ]
    },
    "permissions": {
      "filesystem": [
        "workspace:read"
      ],
      "network": [],
      "secrets": [],
      "desktop": []
    },
    "contributes": {
      "nativeTools": [
        {
          "id": "document",
          "runtime": "builtin:document"
        }
      ]
    }
  }
}
